---
inclusion: auto
---

# 설계도 완전성 검증 체크리스트 (매 작업 시 자동 적용)

> 이 steering은 `inclusion: auto`로 설정되어 모든 대화에 자동 로드됩니다.
> KIRO가 `services/*/docs/` 하위에 산출물을 생성·수정할 때마다 아래 검증을 수행하세요.
> 작업자가 매번 요청하지 않아도 KIRO가 자동으로 체크합니다.

---

## A. 산출물 생성 시 필수 검증 (매 Step 완료 시)

### A-1. 코드 기반 완전성 검증
- [ ] 2-Pass 스캔을 수행했는가? (Pass 1: 구조 스캔 → Pass 2: 정밀 분석)
- [ ] ❌ 판정 전 6회 검색 최소 검증을 수행했는가? (grepSearch 3회 + fileSearch 2회 + 설정파일 1회)
- [ ] 누락 검증 로그 테이블을 산출물 하단에 포함했는가?
- [ ] 멀티 모듈(3개+) 서비스에서 모듈별 분할 분석을 수행했는가?

### A-2. 사용자 추가 정보 반영 검증
- [ ] `runtime-data.md` (수작업 수집 런타임 데이터)가 존재하면 해당 내용을 산출물에 반영했는가?
- [ ] 작업자가 채팅에서 추가로 알려준 정보(인터뷰 결과, 운영 환경 등)를 산출물에 반영했는가?
- [ ] 반영한 추가 정보에 `[작업자 제공]` 또는 `[인터뷰 결과]` 태그를 붙였는가?
- [ ] `glossary.md` 용어 사전의 정규화된 명칭을 사용했는가?

### A-3. Step 간 교차 검증
- [ ] 이전 Step 산출물과 ID/명칭/경로 참조가 일치하는가?
- [ ] 교차 검증 로그 테이블을 산출물 하단에 포함했는가?
- [ ] 불일치 발견 시 이전 산출물을 보정하고 `[교차검증 보정]` 태그를 붙였는가?

### A-4. 설계도 양식 완전성 검증
- [ ] 모든 항목에 확인 상태(✅/⚠️/❌/🔍)를 표기했는가?
- [ ] 모든 항목에 분석 주체 태그([KIRO]/[KIRO+도구]/[KIRO+확정]/[인터뷰])를 표기했는가?

### A-5. 완전성 자가진단 리포트 (매 Step 산출물 하단 필수 포함)

> 비개발자 작업자가 산출물 품질을 판단하기 어려운 문제를 해결하기 위해,
> KIRO가 매 Step 완료 시 산출물 하단에 아래 형식의 자가진단 리포트를 자동 생성한다.
> 작업자는 신뢰도 등급과 추가 검증 프롬프트만 확인하면 된다.

#### 자가진단 리포트 형식 (산출물 하단에 삽입)

```markdown
---
## 🔍 완전성 자가진단 리포트

### 분석 커버리지
| 지표 | 값 | 판정 |
|---|---|---|
| 분석 대상 파일 수 (Pass 1 스캔) | {N}개 | — |
| 실제 읽은 파일 수 (Pass 2 정밀) | {M}개 | {✅ 60%+ / ⚠️ 40~60% / 🔴 40% 미만} |
| ❌ 해당없음 판정 항목 수 | {X}개 | {✅ 3개 이하 / ⚠️ 4~7개 / 🔴 8개+} |
| 🔍 인터뷰필요 판정 항목 수 | {Y}개 | — (참고) |
| 항목 수 3개 이하 카테고리 수 | {Z}개 | {✅ 0개 / ⚠️ 1~2개 / 🔴 3개+} |
| 컨텍스트 윈도우 리셋 횟수 | {R}회 | {✅ 0회 / ⚠️ 1회 / 🔴 2회+} |

### 신뢰도 등급
| 등급 | 조건 (하나라도 해당 시 해당 등급) |
|---|---|
| 🟢 HIGH | 파일 커버리지 60%+, ❌ 3개 이하, 빈약 카테고리 0개, 리셋 0회 |
| 🟡 MEDIUM | 파일 커버리지 40~60%, 또는 ❌ 4~7개, 또는 빈약 카테고리 1~2개, 또는 리셋 1회 |
| 🔴 LOW | 파일 커버리지 40% 미만, 또는 ❌ 8개+, 또는 빈약 카테고리 3개+, 또는 리셋 2회+ |

> **이 산출물의 신뢰도: {🟢/🟡/🔴} {등급명}**

### 🟡/🔴 시 추가 검증 프롬프트 (복사해서 채팅에 붙여넣기)
1. (❌ 해당없음 재검증) `❌ 해당없음으로 판정된 {X}개 항목을 재검증해줘. 특히 {누락 의심 카테고리 나열}.`
2. (미분석 파일 추가 스캔) `분석하지 못한 파일 {N-M}개 중 주요 파일을 추가 스캔해줘. 특히 {미분석 디렉토리 나열}.`
3. (빈약 카테고리 집중 분석) `이 산출물에서 항목 수가 3개 이하인 카테고리({빈약 카테고리 나열})를 집중 재분석해줘.`
4. (컨텍스트 리셋 보완) `이전 세션에서 분석한 내용과 이번 산출물을 교차 확인해줘. 누락된 항목이 있으면 보완해줘.`
```

#### 자가진단 리포트 생성 규칙

1. **매 Step 산출물 생성 시 필수**: `services/*/docs/` 하위에 산출물을 생성할 때마다 해당 산출물 파일 하단에 자가진단 리포트를 포함하라
2. **지표 수집 방법**:
   - "분석 대상 파일 수": Pass 1에서 `listDirectory` + `fileSearch`로 식별한 파일 수
   - "실제 읽은 파일 수": Pass 2에서 `readCode`/`readFile`로 실제 내용을 읽은 파일 수
   - "❌ 해당없음 판정 수": 산출물 내 ❌ 표기 항목 수
   - "항목 수 3개 이하 카테고리": 산출물 내 각 섹션/카테고리별 항목 수를 세어 3개 이하인 것
   - "컨텍스트 윈도우 리셋 횟수": 대화 중 새 세션으로 전환된 횟수 (0이면 단일 세션 완료)
3. **신뢰도 등급 산정**: 위 조건 테이블에 따라 가장 낮은 등급을 최종 등급으로 채택
4. **추가 검증 프롬프트 자동 생성**: 🟡/🔴 등급일 때 구체적인 카테고리명, 디렉토리명, 항목명을 포함하여 작성
5. **🟢 HIGH일 때**: "✅ 신뢰도 HIGH — 추가 검증 불필요. 다음 Step으로 진행하세요." 표기
6. **🔴 LOW일 때**: "⚠️ 신뢰도 LOW — 아래 프롬프트를 순서대로 실행하여 보완 후 진행하세요." 경고 표시

---

## B. 멀티 Repo / 이종 스택 Call Flow 검증 (Phase 2 View 작성 시)

### B-1. 앱↔서버 Call Flow 연결 검증
- [ ] 앱 코드 API 호출부(Retrofit/Alamofire/fetch)와 서버 Controller 엔드포인트가 교차 매핑되었는가?
- [ ] 앱 Repo와 서버 Repo가 분리된 경우, 양쪽 소스 위치를 병기했는가?
- [ ] 요청/응답 DTO 필드 레벨 일치 여부를 확인했는가? (불일치 시 ⚠️ 표기)

### B-2. 서버↔서버 내부 호출 체인 검증
- [ ] 서버 A의 Feign/RestTemplate 호출 URL과 서버 B의 Controller 엔드포인트 실존 여부를 교차 검증했는가?
- [ ] 서버 간 DTO/스키마 필드 레벨 일치 여부를 확인했는가? (불일치 시 ⚠️ 표기)
- [ ] 서버 간 인증 토큰 전파 체인을 검증했는가? (토큰 유실/변환 구간 식별)
- [ ] 3-hop 이상 호출 체인의 타임아웃·리트라이·서킷브레이커 설정 일관성을 검증했는가?

### B-3. 소스 없는 구간 처리 검증
- [ ] 소스 없는 서버(🔍)의 앞뒤 호출 구간을 식별했는가?
- [ ] 앞단(호출자) 코드에서 요청 파라미터·URL·프로토콜을 추출했는가?
- [ ] 뒷단(피호출자) 코드에서 응답 파싱·에러 처리 로직을 추출했는가?
- [ ] Call Flow 다이어그램에서 소스 없는 구간을 [블랙박스/추정] 태그 + 점선으로 표기했는가?

### B-4. OS/네이티브 ↔ 앱 레이어 경계 검증
- [ ] 네이티브(C/C++/Swift/Kotlin)와 앱(JS/Dart) 사이 브릿지 호출을 식별했는가?
- [ ] STB TIF→CAS→DRM 연동 등 OS 레이어↔앱 레이어 경계 Call Flow를 추적했는가?
- [ ] 모바일 생체인증(BiometricPrompt/LAContext)→Keystore/Keychain→서버 인증 체인을 추적했는가?

---

## C. Phase 간 역동기화 검증 (후속 Phase에서 발견한 내용의 이전 Phase 반영)

### C-1. Phase 2 → Phase 1 역반영
- [ ] 아키텍처 View 작성 중 발견한 누락/불일치가 Phase 1 extraction 산출물에 반영되었는가?
- [ ] Call Flow View에서 발견한 서비스 간 호출 관계가 Step 03 API 인벤토리에 반영되었는가?
- [ ] 비즈니스 규칙 View에서 발견한 규칙 충돌/중복이 Step 04 산출물에 반영되었는가?
- [ ] 보안 View에서 발견한 보안 갭이 Step 05 산출물에 반영되었는가?

### C-2. Phase 3 → Phase 1/2 역반영
- [ ] Gap 분석에서 발견한 누락 항목이 해당 Phase 산출물에 반영되었는가?
- [ ] 서비스 간 교차 검증에서 발견한 불일치가 양쪽 서비스 산출물에 반영되었는가?

### C-3. 역반영 추적
- [ ] 역반영한 항목에 `[역반영: Phase X → Phase Y]` 태그를 붙였는가?
- [ ] `_context-notes.md`에 역반영 사유와 변경 내용을 기록했는가?

---

## D. 산출물 품질 검증

### D-1. 다이어그램 품질
- [ ] 모든 Mermaid 다이어그램이 렌더링 가능한가? (문법 오류 없음)
- [ ] 코드 없는 구성 요소에 스테레오타입(`<<binary SDK>>`, `<<no-source>>`, `<<console>>`, `<<external-db>>`, `<<external>>`)을 표기했는가?
- [ ] Dead Code(Step 1b 판정)가 다이어그램에서 제외 또는 구분 표기되었는가?

### D-2. 산출물 완전성
- [ ] 산출물 파일이 올바른 경로(`services/{서비스명}/docs/`)에 생성되었는가?
- [ ] 산출물 파일명이 템플릿 규칙을 따르는가?
- [ ] 산출물 하단에 누락 검증 로그 + 교차 검증 로그가 포함되었는가?

---

## E. 문서 간 정합성 검증 (프로젝트 문서 수정 시)

> `project/` 또는 `.kiro/steering/` 하위 문서를 수정할 때마다 아래 교차 참조 일관성을 검증하세요.

### E-1. 핵심 문서 간 교차 참조 체크
- [ ] `extraction-checklist.md` ↔ `workplan-roadmap.md`: Phase/View 번호·명칭·개수 일치
- [ ] `extraction-checklist.md` ↔ `cdr-design-checklist.md`: CDR 항목 번호 참조 일치
- [ ] `extraction-checklist.md` ↔ `.kiro/steering/09-architecture-views.md`: View 번호·명칭·완료 기준 일치
- [ ] `workplan-roadmap.md` ↔ `README.md`: Phase 설명·View 개수·산출물 목록 일치
- [ ] `.kiro/steering/*.md` 간: 동일 개념에 대한 설명·규칙 일치

### E-2. 수치/개수 정합성
- [ ] Phase 2 View 개수: extraction-checklist = workplan-roadmap = README = steering (현재 15개: 2-1~2-14 + 2-4b)
- [ ] 핵심 Journey 개수: extraction-checklist 2-4b = steering 09 (현재 최소 9개)
- [ ] Phase 1 산출물 목록 개수: extraction-checklist = workplan-roadmap

---

## 적용 시점 요약

| 검증 섹션 | 적용 시점 |
|---|---|
| A. 산출물 생성 시 필수 검증 | `services/*/docs/` 하위 산출물 생성·수정 시 매번 |
| B. 멀티 Repo / 이종 스택 검증 | Phase 2 View 작성 시 (특히 2-4, 2-4b, 2-13) |
| C. Phase 간 역동기화 검증 | 후속 Phase에서 이전 Phase 산출물에 영향 주는 발견 시 |
| D. 산출물 품질 검증 | 모든 산출물 생성·수정 시 |
| E. 문서 간 정합성 검증 | `project/` 또는 `.kiro/steering/` 문서 수정 시 |
