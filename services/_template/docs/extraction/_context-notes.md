# 컨텍스트 노트 — {서비스명}

> 이 파일은 각 Step 진행 중 발견한 핵심 인사이트, 주의사항, 이전 Step 산출물 업데이트 이력을 기록합니다.
> 새 채팅 세션에서 Step을 시작할 때 이 파일을 먼저 읽어 컨텍스트를 복원하세요.

## 서비스 특이사항

> 이 서비스에서 특히 주의해야 할 점을 기록하세요.

- (예: 이 서비스는 레거시 모놀리스에서 MSA로 전환 중이며, 일부 모듈은 구 아키텍처를 따름)
- (예: 소스코드가 없는 외부 연동 서버가 3개 있어 Call Flow에 블랙박스 구간이 존재)

## Step별 핵심 발견사항

### Step 01: 코드 구조
- 발견한 기술 스택: (예: Java 17 + Spring Boot 3.1, Kotlin + Jetpack Compose)
- 아키텍처 패턴: (예: Layered, Hexagonal, MSA)
- 특이 디렉토리 구조: (예: 멀티 모듈 Gradle, 모노레포)
- 환경별 설정 차이 핵심: (예: prod만 Redis 캐시 활성화)
- 다음 Step 주의사항: (예: MyBatis XML이 비표준 경로에 위치)
- 역방향 피드백 필요 항목: (예: 없음)

### Step 01b: Dead Code
- Dead Code 규모: (예: 클래스 23개, API 5개 Dead 판정)
- 주요 Dead 판정 항목: (예: LegacyPaymentModule 전체, OldBatchJob 3개)
- 조건부 Dead(⚠️) 주의 항목: (예: AdminController — 외부 호출 가능성)
- 다음 Step 주의사항: (예: Step 02~08에서 위 Dead 항목 제외 필요)
- 역방향 피드백 필요 항목: (예: 없음)

### Step 01c: 공통 모듈
- 공통 모듈 그룹 요약: (예: 인증 공통 3개, 유틸리티 8개, 공통 DTO 5개)
- 영향 범위 🔴 High 모듈: (예: CommonAuthUtil — 참조 42개)
- 역의존(⚠️ 아키텍처 위반) 발견: (예: core 패키지가 service 패키지 import)
- 사내 공통 라이브러리: (예: company-auth-sdk v2.3, company-crypto v1.1)
- 다음 Step 주의사항: (예: Step 04에서 공통 예외 처리 모듈 구분 필요)
- 역방향 피드백 필요 항목: (예: 없음)

### Step 02: 화면 인벤토리
- 화면 총 수: (예: iOS 45개, Android 48개, 공통 42개, 플랫폼 전용 9개)
- 플랫폼 전용 화면(⚠️): (예: Android에만 위젯 설정 화면 3개)
- 화면명 정규화 이슈: (예: "홈"과 "메인"이 혼용 — "홈"으로 통일)
- 다음 Step 주의사항: (예: Step 04 Use Case에서 화면 ID 교차 매핑 필요)
- 역방향 피드백 필요 항목: (예: 없음)

### Step 03: API & 데이터
- API 총 수: (예: REST 87개, 내부 Feign 12개, gRPC 3개)
- DB 테이블 수: (예: 45개 테이블, ERD 작성 완료)
- 서비스 간 호출 관계 핵심: (예: AuthService → UserService → NotificationService 체인)
- Deprecated/무버전 API: (예: /api/old/* 경로 5개 Deprecated)
- 다음 Step 주의사항: (예: MyBatis XML 동적 SQL이 복잡 — Step 06 성능 분석 시 주의)
- 역방향 피드백 필요 항목: (예: 없음)

### Step 04: 비즈니스 로직
- 비즈니스 규칙 수: (예: 32개 규칙 식별, 하드코딩 매직넘버 8개)
- 상태 전이 핵심: (예: 주문 상태 7단계, 결제 상태 5단계)
- E2E Call Flow 주요 발견: (예: 결제 흐름에서 PG 연동 타임아웃 5초, fallback 없음)
- Use Case 수: (예: 공통 UC 15개, 서비스 특화 UC 8개)
- 배치 작업: (예: 일배치 3개, 시간배치 2개)
- 다음 Step 주의사항: (예: Step 05에서 에러 응답 내 개인정보 노출 확인 필요)
- 역방향 피드백 필요 항목: (예: Step 03 API 인벤토리에 내부 호출 2개 추가 필요)

### Step 05: 보안
- 크리덴셜 위험: (예: 🔴 Critical 2건 — application.yml 평문 DB 패스워드)
- 개인정보 처리 현황: (예: PII 필드 15개 식별, 암호화 미적용 3개)
- Security Layer 준수율: (예: 217항목 중 준수 142, 부분 35, 미준수 28, 해당없음 12)
- 앱 쉴딩: (예: ProGuard 적용, 루팅 탐지 있음, 위변조 탐지 없음)
- 다음 Step 주의사항: (예: Step 06에서 보안 테스트 존재 여부 확인 필요)
- 역방향 피드백 필요 항목: (예: Step 03 API 인벤토리에 인증 미적용 API 3개 표기 필요)

### Step 06: 품질
- 테스트 현황: (예: 단위 230개, 통합 15개, E2E 0개)
- 고위험 영역: (예: PaymentService — 변경 빈도 높음 + 테스트 0개)
- 의존성 건강도: (예: CVE 🔴 2개, EOL 라이브러리 3개)
- 성능 병목 후보: (예: N+1 쿼리 5건, 루프 내 외부 호출 2건)
- 다음 Step 주의사항: (예: 없음)
- 역방향 피드백 필요 항목: (예: Step 04 비즈니스 규칙에 경계값 3개 추가 필요)

### Step 07: UX
- 인터랙션 패턴 수: (예: 화면별 이벤트 핸들러 총 156개)
- 검증 규칙: (예: 입력 필드 42개, 프론트만 검증 8개 — 백엔드 검증 누락)
- 딥링크 경로: (예: 12개 딥링크 경로, 인증 체크 누락 2개)
- 피처 플래그: (예: 활성 5개, 실험 종료 후 미정리 2개)
- 다음 Step 주의사항: (예: 없음)
- 역방향 피드백 필요 항목: (예: Step 02 화면 인벤토리에 로딩/에러 상태 화면 5개 추가 필요)

### Step 08: 인프라
- 외부 연동 시스템: (예: 8개 — PG, CRM, 인증, 푸시, CDN 등)
- 배포 환경: (예: k8s 3개 환경, 인스턴스 수 🔍 인터뷰필요)
- STB 관련: (예: 해당 없음 / 기종 3개, 저사양 메모리 제약 512MB)
- NFR 설정: (예: 타임아웃 5초, 커넥션 풀 20, 캐시 TTL 300초)
- 다음 Step 주의사항: (예: Step 09에서 외부 연동 SLA 미확인 항목 🔍 표기 필요)
- 역방향 피드백 필요 항목: (예: Step 03 API 인벤토리에 배치 호출 API 2개 추가 필요)

## 역방향 피드백 이력

> 나중 Step에서 이전 Step 산출물을 수정한 경우 여기에 기록하세요.

| 날짜 | 수정한 Step | 수정된 산출물 | 변경 사유 |
|---|---|---|---|
| | | | |

## Phase 2 진입 시 주의사항

> Phase 1 완료 후, Phase 2 View 생성 시 이 서비스에서 특히 주의할 점을 기록하세요.

- 
